/*
** EPITECH PROJECT, 2020
** zappy
** File description:
** zappy
*/

#include "protocol.h"
#include "game.h"

int __proto_mct(const char *param, const int32_t fd, __player_t *player)
{
    for (int i = 0; __map->__cells[i] != NULL; i++) {
        __proto_bct(param, fd, player);
    }
    return (0);
}
